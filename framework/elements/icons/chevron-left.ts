import Icon, { FontVariationSettings } from "./icon";

export default class ChevronLeft extends Icon {
  constructor (
    fontVariationSettings?: FontVariationSettings
  ) {
    super('chevron_left', fontVariationSettings);
  }
}