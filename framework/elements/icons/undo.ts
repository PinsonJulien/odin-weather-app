import Icon, { FontVariationSettings } from "./icon";

export default class Undo extends Icon {
  constructor (
    fontVariationSettings?: FontVariationSettings
  ) {
    super('undo', fontVariationSettings);
  }
}