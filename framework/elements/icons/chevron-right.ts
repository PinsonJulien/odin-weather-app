import Icon, { FontVariationSettings } from "./icon";

export default class ChevronRight extends Icon {
  constructor (
    fontVariationSettings?: FontVariationSettings
  ) {
    super('chevron_right', fontVariationSettings);
  }
}